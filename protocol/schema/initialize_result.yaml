$schema: "http://json-schema.org/draft-07/schema#"
$id: initialize_result.yaml
title: InitializeResult
type: object
required: [protocolVersion, source, capabilities]
properties:
  protocolVersion: {type: string}
  source:
    type: object
    required: [id, name, version]
    properties:
      id: {type: string}
      name: {type: string}
      version: {type: string}
    additionalProperties: false
  capabilities: {$ref: "capabilities.yaml"}
additionalProperties: false
